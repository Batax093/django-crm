version: 2
builds:
  - name: Build
    command: |
      python manage.py collectstatic --noinput
      python manage.py migrate
  - name: start
    command: gunicorn djcrm.wsgi:application --bind 0.0.0.0:$PORT
    port: $PORT
routes:
  - src: "/static/(.*)"
    dest: "/static/$1"
