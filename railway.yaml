version: 2
builds:
  - name: Build
    command: |
      python manage.py collectstatic --noinput
      python manage.py migrate
  - name: Start
    command: sh runserver.sh
    port: $PORT
routes:
  - src: "/static/(.*)"
    dest: "/static/$1"
